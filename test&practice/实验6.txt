一、虚函数

1. 将实验5中vehicle类中的Print()、Run()和stop()函数改为纯虚函数

2. 参照P264定义fun函数，参数为vehicle类的指针，通过指针调用print()、Run()和Stop()函数

3. 主程序内声明vehicle类的指针*p，定义3个类的对象并且初始化（vehicle是抽象类，不能定义对象），分别将指针指向3个对象后，以指针为实参调用fun函数。注意与实验5结果的不同


二、运算符重载与类模板

仿照P355例9-3定义数组类模板

1、实现[]和=重载；

2、增加重载+，-实现两个数组对应元素的加减操作（首先判断数组大小是否相等，若不相等则不做处理）

3、仿照p314例8-3，重载输出符<<，实现直接通过cout<<，输出数组类对象中动态数组的所有元素值

4、主函数中定义不同类型的数组对象，实现上述各种运算






